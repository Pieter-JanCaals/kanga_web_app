<h1>Events#show</h1>

<div
  id="map"
  style="width: 100%; height: 600px"
  data-markers="<%= @bars.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>

<p><%= @event.closest_bar.eta %></p>

<%= link_to "Back", event_drinks_path(@event), class: "btn btn-primary" %>

<%= javascript_pack_tag 'mapbox' %>
