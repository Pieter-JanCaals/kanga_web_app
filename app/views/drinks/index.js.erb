

document.getElementById("drink-list").innerHTML = "<%= j render('/drinks/drinks', drinks: @drinks, order: @order) %>";
document.getElementById('drink-title').innerText = "<%= j @category.nil? ? 'Popular Drinks' : @category.name %>"

// <%= j render('test.js.erb') %>
// document.getElementById('eta-btn').click();

// const orderAmounts = Array.from(document.querySelectorAll(".order-amount"));
// const total = orderAmounts.reduce((accum, element) => {
//   return accum + Number.parseInt(element.value)
// }, 0);
// let $cartQty = total;

document.querySelector("#shopping-cart-btn .badge").innerText = "<%= @current_order.order_drinks.sum(:amount) %>";

window.initializeCartAnimation();
